# Swarm configuration — auto-generated by /init
# Model auto-detected from your current session: glm-5

models:
  orchestrator: glm-5

workers:
  - name: builder
    model: glm-5
    capabilities: [code, test]
    count: 2
  - name: reviewer
    model: glm-5
    capabilities: [review, research]
    count: 1

budget:
  totalTokens: 5000000
  maxCost: 10.0
  maxConcurrency: 2

quality:
  enabled: true

features:
  planning: true
  verification: true
